# Changelog

版本以功能為單位小步發版；每版一句話。安裝包在 [Releases](https://github.com/TipsyDrifter/claude-usage-monitor/releases)。

## 1.8.11 — 2026-09-25

- 新：全量視角——帳號下拉與趨勢／歷史頁的座位 chip 多一個「全部帳號（不分帳號）」：趨勢頁把所有帳號的配對區間合起來算，期間消耗／撞牆／來源歸因用本機全量（含最早觀測之前的用量與雲端 session）；歷史頁把各帳號的窗口合成一條時間軸、明細多「帳號」欄；證據匯出同樣支援。今日頁與懸浮窗仍看目前登入的帳號（額度是每個帳號各自的）。

## 1.8.10 — 2026-09-25

- 新：「接近警戒時的查額度間隔」可調（預設 2 分鐘）；觸發門檻改跟你在「告警」設的警戒值連動，不再寫死 90%。
- 改：趨勢頁來源歸因不到 1% 的來源顯示「<1%」，不再被進位成 0%。

## 1.8.9 — 2026-09-25

- 新：設定頁「採集節奏」多一條「查額度最少間隔」（預設 10 分鐘、可調 1～30）；以前寫死。
- 改：毛玻璃外觀的大數字改成在帳號行與數值條之間置中。
- 改：同一個帳號的 Desktop 數字會補位顯示（最新的贏，重置時刻沿用 CLI 的）；不同帳號仍分開。
- 修：切換 CLI 帳號後，CLI 有時會連續好幾個小時回報「上一個帳號」的額度——現在會擋下來不記、不顯示，並在畫面說明；以前會把舊帳號的觸頂記到新帳號頭上。

## 1.8.8 — 2026-09-25

- 修：Claude Desktop 的用量歷史檔找不到——Desktop 是 MSIX 應用，它的檔案實際在 `%LOCALAPPDATA%\Packages\Claude_*\LocalCache\Roaming\Claude\`，安裝版從 `%APPDATA%\Claude\` 找不到就靜靜跳過（1.8.3 起 Desktop 這條管道等於沒有）。現在兩處都找、取最新的，並在採集紀錄寫下找到的路徑。
- 修：額度查詢失敗時的等待時間不看你在不在用——以前失敗幾次就等 40～60 分鐘，一直在用也不刷新。現在有在用時最多等 10 分鐘。
- 修：查詢連續失敗、或 CLI 回 0% 但 Desktop 同帳號有數字時，畫面會直接說明並告訴你怎麼處理（重新 `/login`）。
- 修：門鈴密碼牌對不上的提醒改成每個來源各自計算——以前 Claude Code hook 一成功就把瀏覽器擴充的錯誤清掉，擴充壞了看不到。
- 修：門鈴「白響」計數只在真的拿到伺服器新值時才算，查詢失敗那幾天不會誤報「瀏覽器可能登著第三帳號」。
- 新：歷史頁多一張「這段期間用了什麼」——翻到哪一天／週／月，就看那段期間的模型與專案分佈（本機記錄、只算這個帳號登入中的時段）。
- 改：設定頁懸浮窗外觀只留名字，拿掉 W1／W4／W5 編號；毛玻璃外觀的標準卡大數字貼著數值條、展開面板整塊向下對齊。

## 1.8.7 — 2026-09-22

- 修：設定頁關掉懸浮窗、再從系統匣點開，拖一下或切密度它就又消失——系統匣的切換現在會寫回設定。
- 修：重開 App 後懸浮窗穿回預設外觀（設定頁仍是你選的）——啟動時視窗問設定問得太早會拿到預設值；現在後端等設定載完才回答，前端也不會 3 秒就放棄。
- 修：趨勢頁切帳號，期間消耗、撞牆次數、來源歸因都不變——以前只按「Claude Code／Desktop」入口過濾，現在按「這個帳號登入中的時段」過濾（依觀測記錄判斷）；最早一筆觀測之前的用量分不出帳號、不計入任何帳號，卡片上會寫。
- 修：資料健康頁的「額度匯率」卡是早期佔位，現在顯示目前帳號 180 天的期間匯率、最近一個月的數字與觀測起點。
- 修：閒置心跳設 0 時，一直在用 Claude Code 也不刷新。三件事一起修——Claude Code hook 的門鈴密碼牌若過期（App 換過牌）啟動時自動換新；本機看到新的對話紀錄就直接去查額度（以前只當「准不准查」的依據）；密碼牌對不上時資料健康頁與設定頁會提醒。採集紀錄的 `refresh-ok` 多記一欄 `cause`（誰叫醒的：門鈴／本機紀錄／手動／啟動／排程）。
- 改：趨勢頁「模型別額度倍率」量條反轉成「每 1% 額度換到的 API 用量」，越長越划算（之前是倒數，越長越傷額度）；$/1% 與區間數字不變。
- 改：設定頁「背景重讀間隔」只講重讀；「最少隔 3 分鐘、沒在用就暫停」搬到「閒置心跳」。

## 1.8.6 — 2026-09-21

- 修（1.8.5 的補強）：存檔改成「後端的設定真的進來了才准存」（有密碼牌才算），init 失敗也鎖著；設定更新的事件監聽改成先掛再問，不漏接後端載完的那次廣播。診斷紀錄多記是哪個視窗送的。

## 1.8.5 — 2026-09-21

- 修：設定視窗在 App 啟動時就先建好，那一刻拿到的是預設值、之後沒人通知它——在設定頁按的每一下都是「預設值＋改動」（1.8.4 之前會蓋掉其他設定，1.8.4 起整筆被丟但畫面顯示已儲存）。現在後端載完設定會廣播給所有視窗，前端拿到還沒載入的設定也會重試。

## 1.8.4 — 2026-09-21

- 修：每次啟動後 0.6 秒內有一筆「還沒載入設定」的早到存檔，把預設值蓋回去——開機自動啟動被關掉（Windows 的 Run 值被刪）、門鈴密碼牌被清空（1.8.2 只擋了密碼牌）。現在前端沒載完不存、後端收到空密碼牌的存檔整筆丟棄。
- 「開機自動啟動」新安裝預設開（App 要常駐帳本才會累積）；既有設定照舊。

## 1.8.3 — 2026-09-21

第一個對外版本：MIT 授權、README 對外版、安裝包中繼資料（publisher／版權／non-affiliation）、CHANGELOG。安裝包未簽章（SmartScreen 會攔，README 有步驟）。

## 1.8.2 — 2026-09-21

- 瀏覽器擴充翻新成「門鈴」：認 claude.ai 目前的送訊 RPC（`ConversationService/PerformAction`），密碼牌改存本機、圖示閃燈、上次響鈴；不上商店，sideload。
- 每次門鈴在 `collector.log` 留一行（含密碼牌錯誤的 401）。
- 修：啟動時前端的早到存檔會把後端剛鑄的門鈴密碼牌清成空字串（兩個門鈴都 401、沒人知道）。
- 設定頁「打包診斷資料」：一個 zip（採集紀錄、遮掉密碼牌的設定、資料健康度、環境資訊）另存到你選的地方。
- 趨勢頁開頁從 5 秒降到毫秒級：統計搬到背景執行緒、結果快取、採集完預熱；統計數字不變。
- 趨勢頁／歷史頁點座位 chip，頂欄帳號跟著換。設定頁「採集節奏」「系統」說明改人話。

## 1.8.1 — 2026-09-20

懸浮窗三張臉：W5 深色儀器（預設）／W1 毛玻璃光暈／W4 環形；設定頁「懸浮窗外觀」三選一。

## 1.7.x — 2026-09-19 · 09-20

文案大眾化：趨勢頁術語改白話，「怎麼算的 ▸」摺疊收術語版；「撞牆值」改「警戒值」。

## 1.6.x — 2026-09-18

分模型倍率改加權非負最小平方回歸（NNLS）＋bootstrap 信心區間；四家族（Fable／Opus／Sonnet／Haiku）綜合費率，旁掛純區間法交叉驗證。

## 1.5.0 — 2026-09-18

歷史頁 5H／7D／FABLE 三條額度切換，7d／Fable 窗口重建；證據匯出多 `windows.csv`。

## 1.4.x — 2026-09-16 · 09-18

有回應的畫面：數字滾動、只有變了的才動、切頁淡入、hover 小標、存檔回執；驗收修正六件。

## 1.3.0 — 2026-09-15

兩個帳號：座位有名字（別名）、懸浮窗與統計視窗切換器、看別的帳號＝最後已知值。

## 1.2.0 — 2026-09-15

兩級告警：留意值／警戒值成全 app 唯一來源（懸浮窗指針與變色、系統匣圖示、系統通知）；同窗每級只響一次。

## 1.1.x — 2026-09-14 · 09-15

清地基（刪 1,700 行舊資料層）、圓角照原型、裁決口徑統一（觸頂時刻＝短窗、重置％＝長窗）。

## 1.0.0 — 2026-09-11

系統匣＋懸浮窗、今天撞牆嗎、這半年（額度匯率、縮水判定、證據匯出）、歷史檢視、資料健康度。
